version: 2

sources:
  - name: jaffle_shop
    database: raw  
    schema: jaffle_shop  
    tables:
      - name: orders
        description: one order per person that buys something smh
        columns:
          - name: order_id
            description: one order_id per person smh
            tests:
              - unique
              - not_null
      - name: customers
        columns:
          - name: id
            tests:
              - unique
              - not_null

  - name: stripe
    tables:
      - name: payments